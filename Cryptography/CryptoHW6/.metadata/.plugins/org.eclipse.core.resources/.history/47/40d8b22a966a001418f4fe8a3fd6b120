package cryptoHW6;

public class EpsFunc {
	public static long EpsFunc(long p) {
		
		long mod = p%5;
		
		if (mod == 4 || mod == 1) {
			return 1;
		}
		else if (mod == 3 || mod == 2) {
			return -1;
		}
		else if (mod == 0) {
			return 0;
		}
		
	}
}
